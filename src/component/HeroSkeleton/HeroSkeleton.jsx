import React from 'react';
import './HeroSkeleton.css'; // Import the CSS file

const HeroSkeleton = () => {
  return (
    <div className="hero-skeleton"></div>
  );
};

export default HeroSkeleton;
